#:kivy 1.9.1

<Browser>:
    StackLayout:
        size: (400, 30)
        pos: (0, 570)


        Button:
            text: "abbb"
            on_press: root.callback1()
            valign: 'middle'

        TreeView:
        	font_size: 20
        	center_x: root.width / 4
        	top: root.top - 50
        	width: 250
        	load_func:root.callback

